language: python
python:
  - "3.7"
cache: pip
install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt
script:
  - make format-check
  - pylint --output-format=text main.py
